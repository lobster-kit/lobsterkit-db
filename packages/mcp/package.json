{
  "name": "@lobsterkit/db-mcp",
  "version": "0.1.0",
  "description": "MCP server for LobsterDB — managed PostgreSQL for agents",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "lobsterdb-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lobsterkit/db": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsup": "^8.3.6",
    "typescript": "^5.7.3"
  }
}
